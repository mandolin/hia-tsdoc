# TSDoc Standalone Example

Runs the TSDoc runner against `src/calculator.ts` and emits generated JavaScript, ordinary source map, TSDoc extraction, JSDoc bridge artifact, HIA document, doc-source-map manifest and a producer result.

```sh
tsdoc --config tsdoc.config.json
```
