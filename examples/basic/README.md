# Basic Example

The first runnable example is the generated-source fixture under:

```text
fixtures/doc-source-map/ts-js
```

Run:

```sh
npm install
npm run build:fixtures
npm run check:fixtures
```

The output demonstrates TypeScript/TSDoc extraction, JS source-map linkage, type-only symbol preservation and HIA adapter output.
