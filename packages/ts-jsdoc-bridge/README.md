# TS JSDoc Bridge

Bridges TSDoc extraction artifacts with JSDoc semantics.

The P1 bridge preserves runtime symbols for JSDoc-compatible pipelines and records type-only symbols as TS-only bridge metadata.
